# 0.3.6 初回起動は空の一覧

初回起動時刻をsettings.cfgに保存し、それより後に作成されたCodexタスクのみ自動取得する。
過去タスクが再実行されても自動追加しない。ディープリンクで明示追加したタスクと登録済みタスクは引き続き同期する。
既存ユーザーの登録済みタスクは消さない。デモはボタンから明示的に表示する。

2026-09-06検証: Python回帰テスト9件成功。初回境界、過去タスク更新の除外、明示追加の例外、新規作成を確認。
Windows Releaseを新しい保存先で起動し、Guaで自動同期中かつ一覧0件を確認。
再起動後に実タスク「Agentarium初回起動後の追加確認」を作成し、その1件だけが自動表示されることをGuaで確認。
Gua撮影: artifacts/screenshots/v036-first-launch-empty.png、v036-only-new-task.png。
